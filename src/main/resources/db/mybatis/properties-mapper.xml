<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trodix.duckcloud.core.persistance.dao.mappers.PropertyMapper">

    <insert id="insertProperty" useGeneratedKeys="true">
        INSERT INTO properties
        (
            node_id, property_name, string_val, serializable_val, long_val, double_val, date_val
        )
        VALUES
        (
            #{parent_id}, #{type_id}
        )
    </insert>

</mapper>